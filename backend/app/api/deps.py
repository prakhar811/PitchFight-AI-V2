"""Shared FastAPI dependencies.

Future responsibility: inject auth context, database sessions, and
service instances into route handlers.

TODO: Add dependencies once authentication and data layers exist.
"""
